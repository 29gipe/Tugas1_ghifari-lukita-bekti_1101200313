from sklearn import datasets

# Memuat dataset iris
iris = datasets.load_iris()

# Menampilkan fitur dari dataset iris
print(iris.feature_names)
print(iris.data[:5])

# Menampilkan target dari dataset iris
print(iris.target_names)
print(iris.target)
